/** Default HTTP timeout for diffusion generate (5 minutes). */
export const DEFAULT_REQUEST_TIMEOUT_MS = 5 * 60 * 1000;

/** Absolute max gen dimension unless profile cap is lower. */
export const DEFAULT_GEN_MAX = 2048;
