# torch: install CUDA build via start.ps1 (not CPU-only pip default)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
diffusers>=0.35.0
transformers>=4.51.0
accelerate>=1.2.0
peft>=0.14.0
bitsandbytes>=0.45.0
safetensors>=0.4.0
pillow>=10.0.0

# Optional (Windows may need VS Build Tools):
# flash-attn>=2.6.0
